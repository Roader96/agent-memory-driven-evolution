# 部署超时：CI 卡在 60 秒超时

> **类型**：incident / 错误记录
> **日期**：2026-06-08

## 症状

CI 流水线在 `npm install` 步骤超时（60s），构建失败。

## 根因

- 网络慢 + 缓存未启用

## 修复

```bash
# 启用 npm 缓存
npm config set cache ~/.npm-cache
```

## 教训

- CI 里的安装步骤要加缓存，否则每次重新下载依赖
- 超时阈值要按网络环境调整